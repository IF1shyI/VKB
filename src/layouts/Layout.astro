---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="sv">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/bilder/logga.svg" />
		<meta name="generator" content={Astro.generator} />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap" rel="stylesheet">
		<title>{title}</title>
	</head>
	<body>
		<slot />
	</body>
</html>
<style is:global>
	:root{
    /* Margin */
    --margin-xxs: .25rem;
    --margin-xs: .5rem;
    --margin-s: .75rem;
    --margin-m: 1rem;
    --margin-l: 1.25rem;
    --margin-xl: 1.75rem;
    --margin-xxl: 2.5rem;

    /* Colors */
    --svart: rgb(0, 0, 0);
    --vit:rgb(255, 255, 255);
    --svart-genomskinlig: rgba(0, 0, 0, 0.5);
	--grått-genomskinlig:rgb(160, 160, 160, 0.5);
    --kontrast:rgb(18, 145, 255);
    --orange:rgb(171, 89, 19);
	--orange-btn-gradient: linear-gradient(to bottom right, rgb(171, 89, 19), rgb(255, 110, 50));
	--green:#50C878;
	--red:#FF0000;

    /* Font */
    --font: "Inter", sans-serif;

	/* Size */
	--header-sz: 4rem;
	--header-btn-sz: calc(var(--header-sz)*0.5);
	--input-sok-sz: 18rem;
	--sz:2rem;
}
	html {
		font-family: var(--font);
		background: var(--svart);
	}
	code {
		font-family:
			Menlo,
			Monaco,
			Lucida Console,
			Liberation Mono,
			DejaVu Sans Mono,
			Bitstream Vera Sans Mono,
			Courier New,
			monospace;
	}

	.hide{
    display: none;
	}

	body{
    margin: 0;
    background-color: black;
    color: #fff;
    height: 100vh;
}
</style>
