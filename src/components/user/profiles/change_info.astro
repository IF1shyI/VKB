<div class="info_container">
  <form id="about_info_container" class="about_info_container"></form>
  <div id="profile_picture" class="profile_picture"></div>
</div>

<script is:inline>
  document.addEventListener("DOMContentLoaded", async () => {
    const jwtToken = localStorage.getItem("jwt");

    if (jwtToken) {
      try {
        // Skicka en begäran till backend för att verifiera JWT och få användarinfo
        const response = await fetch("http://127.0.0.1:5000/checksession", {
          method: "GET",
          headers: {
            Authorization: `Bearer ${jwtToken}`, // Skicka token i Authorization-headern
          },
        });

        if (response.ok) {
          const data = await response.json();

          // Om token är giltig och användarnamn finns, uppdatera knappen
          if (data.message) {
            console.log(data.message);
          }
        } else {
          // Om token inte är giltig, ta bort den
          localStorage.removeItem("jwt");
        }
      } catch (error) {
        console.error("Fel vid validering av session:", error);
        localStorage.removeItem("jwt");
      }
    }
  });
</script>

<style>
  .info_container {
    display: flex;
    flex-wrap: wrap;
    margin-top: var(--margin-m);
    gap: 20px; /* Lägger mellanrum mellan huvudsektionerna */
  }

  .about_info_container {
    width: 70%;
    display: flex;
    flex-direction: column;
    gap: 20px; /* Mellanrum mellan rader */
  }

  .input_label_holder {
    display: flex;
    flex-direction: column;
    margin-bottom: 15px;
    width: 100%;
  }

  .info_label {
    font-size: 1.2rem;
    margin-bottom: 5px; /* Mellanrum mellan label och input */
    color: white; /* Justera färg så den är läsbar */
  }

  input {
    padding: 8px;
    font-size: 1rem;
    border: 1px solid #ccc;
    border-radius: 4px;
    width: 100%; /* Gör input-fälten bredare */
    box-sizing: border-box; /* Ser till att padding inte påverkar bredden */
  }

  button {
    padding: 10px;
    font-size: 1rem;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    background-color: #ff9500;
    color: white;
    text-align: center;
    width: 200px;
    margin: 0; /* Nollställ margin */
  }

  button:hover {
    background-color: #e07c00;
  }

  .button_container {
    display: flex; /* Gör knapparna horisontella */
    gap: 20px; /* Mellanrum mellan knapparna */
  }

  .profile_picture {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
    padding-left: 30px;
  }

  .profile_picture img {
    width: 150px;
    height: 150px;
    border-radius: 50%;
    object-fit: cover;
  }

  h5 {
    margin: 0;
    padding-bottom: 10px;
    font-size: 1.5rem; /* Gör rubriken större */
    color: white;
    font-weight: bold; /* Gör texten fet */
  }
</style>
