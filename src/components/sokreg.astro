<label class="reg-num-label" for="reg-number">Sök reg nummer:</label>
<div class="input-wrapper">
  <input
  type="text"
  id="reg-number"
  class="registration-input"
  placeholder="ABC123"
  maxlength="6"
/>
<div onclick="Search()"><img src="/bilder/arrow_r.svg" alt="arrow" class="sok-knapp" id="sok-knapp"></div>
</div>
<div id="loading-message" class="loading">Hämtar data...</div>
<div class="step-two">
  <div class="car-info" id="car-info"></div>
  <label class="mile-label" for="milage-input">Hur många mil per månad tänte du åka?:</label>
  <div class="input-wrapper">
    <input
    type="text"
    id="milage-input"
    class="milage-input"
    placeholder="1000"
    />
    <div onclick="Calc()"><img src="/bilder/arrow_r.svg" alt="arrow" class="sok-knapp" id="sok-knapp"></div>
  </div>
  <div id="loading-message" class="loading">Hämtar data...</div>
  <div id="fuel-find" class="loading">Hämtar bränslepriser...</div>
  <div id="ins-find" class="loading">Hämtar försäkring...</div>
  <div id="maintenance-find" class="loading">Räknar på underhåll...</div>
  <div id="nklar" class="loading">Nästan klar...</div>
</div>


<script src="/js/sok.js" is:inline></script>



<style>

  .input-wrapper{
    position: absolute;
    width: var(--input-sok-sz);
    aspect-ratio: 4/1;
    padding-right: 4rem;
    border-radius: 4px;

    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);

    overflow: hidden;
  
    box-shadow: 0 4px 8px 2px rgba(0, 0, 0, 0.2);
    border: 2px solid #000;
}


    input{
      width: var(--input-sok-sz);
      height: 100%;

      position: absolute;
      left: 0;
      top: 0;

      font-size: 2rem;
      font-weight: 600;

      text-align: center;
      letter-spacing: 5px;
      font-family: var(--font);
      text-transform: uppercase;
      padding: 0;
      border: 0;
    }

  .reg-num-label{
    position: absolute;
    top: 20%;
    left: 50%;

    transform: translate(-50%, -50%);
  }
    img{
      position: absolute;
      left: var(--input-sok-sz);
      background-image: var(--orange-btn-gradient);
      top: 50%;
      transform: translateY(-50%);
      background-size: contain;
      background-repeat: no-repeat;
      background-position: right center;
      height: 100%;
      cursor: pointer;
      z-index: 60;
    }

    img:hover{
      opacity: 0.8;
    }
  
    
    .registration-input::placeholder {
        color: rgba(0, 0, 0, 0.5);
    }

    .loading {
            display: none;
            position: absolute;
            top: 80%;
            left: 50%;
            transform: translate(-50%, -50%);

            font-size: 2rem;

            font-family: var(--font);
            color: var(--kontrast);
        }

  .step-two {
    display: none;
    position: absolute;
    width: 100%;
    height: 100%;
  }
  .step-two.show {
    display: flex;
    position: absolute;
    width: 100%;
    height: 100%;

    top: 0;
    left: 0;
  }

  .car-info{
    position: absolute;
    width: 100%;
    height: fit-content;

    top: 110%;
    left: 0;

    font-family: var(--font);
    font-size: 1.5rem;

  }

  .mile-label{
    position: absolute;
    top: 20%;
    left: 50%;

    transform: translate(-50%, -50%);

    width: 90%;
    font-size: 1rem;
  }

  @media (max-width: 600px) {
  .input-wrapper {
    width: 70%; /* Justera bredden här */
    padding-right: 2rem; /* Minska padding om nödvändigt */
  }

  input {
    font-size: 1.5rem; /* Justera fontstorlek */
    width: 80%;
  }

  .reg-num-label, .mile-label {
    font-size: 0.8rem; /* Justera storleken på etiketterna */
  }

  img {
    height: 100%; /* Justera storleken på ikonen */
    left: 80%; /* Placera om ikonen */
  }
}

</style>
